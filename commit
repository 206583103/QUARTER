In app.yml add-->

 branchPath: "refs/heads/github-service" in Git tag
 
 in GetService add-->
 fiels->

@Value("${git.branchPath}")
	String BRANCHPATH;

and method

contents = contentService.getContents(repo, fileName, BRANCHPATH);

replacing 

contents = contentService.getContents(repo, fileName);
